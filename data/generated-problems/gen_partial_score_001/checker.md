## 判题逻辑

这是一个普通的整数比较题。判题程序会读取选手输出和标准答案文件，比较两者是否严格相等（忽略空白字符）。

### 判题流程
1. 读取选手输出文件 `output.txt` 中的第一个整数（忽略前后空白）。
2. 读取答案文件 `answer.txt` 中的第一个整数。
3. 如果两个整数相等，返回 `Accepted`；否则返回 `Wrong Answer`。

### 注意事项
- 如果选手输出不是合法的整数，判为错误；
- 不允许输出任何多余信息（但允许末尾的空白）。
- 答案保证在 `long long` 范围内。