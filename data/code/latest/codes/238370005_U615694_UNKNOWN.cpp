#include<bits/stdc++.h>
#pragma GCC optimize(2)
#define int long long
using namespace std;
signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int n,k,p;
	cin>>n>>k>>p;
	if(k==0)
	{
		cout<<(1+n)*n/2;
	 } 
	 
	return 0;
}

